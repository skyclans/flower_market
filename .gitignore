# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# OS
.DS_Store
Thumbs.db

# 참고: data/ 는 의도적으로 커밋한다 (Phase 0 PoC 산출물).
#   대규모 운영 적재 단계에서는 raw/ 를 외부 스토리지(S3)로 분리 권장.

# 대용량 원본 엑셀 — 재수집 가능하므로 깃 제외 (데이터는 Neon에 적재됨)
data/raw/
