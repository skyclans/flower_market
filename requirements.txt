# Phase 0 — 수집/정규화
requests>=2.31
pandas>=2.0
xlrd>=2.0.1          # .xls (OLE) 파싱
openpyxl>=3.1        # .xlsx 호환

# Phase 1 — 적재/배치
psycopg[binary]>=3.1
psycopg_pool>=3.2

# Phase 1 — API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
